<h1>Editing friend</h1>
<hr>

<%= render "form", friend: @friend %>

<br>

<div>
  <%= link_to "Show", @friend, class: "btn btn-dark btn" %>
  <%= link_to "Delete", @friend, method: :delete, data: { "turbo-method": :delete, confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, class: "btn btn-danger"%>
  <%= link_to "Back", @friend, class: "btn btn-dark"%>
</div>
